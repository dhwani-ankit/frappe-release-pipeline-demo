# ⚠️ THIS WORKFLOW IS DISABLED
# Using release.yml instead for the release pipeline
# This file is kept for reference only

# To completely disable, you can delete this file or rename it to deploy.yml.disabled

name: Deploy to Server (Disabled)

# Disabled - no triggers enabled
# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# Empty workflow that never runs
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled - use release.yml instead'
        required: false
        default: 'disabled'

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "⚠️ This workflow is disabled"
          echo "Use .github/workflows/release.yml instead"
